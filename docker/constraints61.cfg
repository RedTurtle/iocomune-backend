# Input di create-constraints.py per la linea Plone 6.1.
# Il path relativo funziona sia dal repo (docker/) sia dentro l'immagine (/dist/docker/).
[buildout]
extends =
    ../plone61.cfg
allow-picked-versions = false

[versions]
sbom4python = 0.12.5
